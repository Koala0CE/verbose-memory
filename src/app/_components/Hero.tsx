import { Stack, Typography } from "@mui/material";
import React from "react";

export const Hero = () => {
  return (
    <Stack>
      <Typography variant="h1">Adventures of Jupi & Elvy</Typography>
    </Stack>
  );
};
